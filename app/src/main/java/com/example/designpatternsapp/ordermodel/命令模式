定义：将一个请求封装成一个对象，从而让用户使用不同的请求把客户端参数化；对请求排队或者记录请求日志，以及支持可撤销的操作
使用场景：
1、需要抽出待执行的动作，然后以参数的形式提供出来——类似于过程设计中的回调机制，而命令模式正式回调机制的一个面向对象的替代品
2、在不同的时刻指定、排列和执行请求。一个命令对象可以有与初始请求无关的生存期
3、需要支持取消操作
4、支持修改日志的功能，这样当系统崩溃时，这些修改可以被重做一遍
5、需要支持事务操作
好处：更弱的耦合性、更灵活的控制性以及更好的扩展性
坏处：类的膨胀，每新增一个命令就要新增一个类